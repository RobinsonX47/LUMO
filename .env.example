# LUMO Environment Variables Configuration
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control!

# ========================================
# SECURITY (REQUIRED IN PRODUCTION)
# ========================================

# Generate a secure random key with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-super-secret-key-change-this-in-production

# Flask environment: development or production
FLASK_ENV=development

# ========================================
# DATABASE (REQUIRED)
# ========================================

# PostgreSQL connection string (Production)
# Format: postgresql://user:password@host:port/database
# DATABASE_URL=postgresql://lumo_user:password@localhost:5432/lumo_db

# SQLite will be used if DATABASE_URL is not set (Development only - NOT for production scale)

# ========================================
# TMDB API (REQUIRED)
# ========================================

# Get your API key from: https://www.themoviedb.org/settings/api
TMDB_API_KEY=your-tmdb-api-key-here

# ========================================
# GOOGLE OAUTH (OPTIONAL)
# ========================================

# Get credentials from: https://console.cloud.google.com/
GOOGLE_CLIENT_ID=your-google-client-id-here
GOOGLE_CLIENT_SECRET=your-google-client-secret-here

# ========================================
# REDIS (OPTIONAL - For Rate Limiting)
# ========================================

# Redis connection string for rate limiting
# If not set, in-memory rate limiting will be used (not recommended for production)
# REDIS_URL=redis://localhost:6379/0
# REDIS_URL=redis://username:password@host:port/db

# ========================================
# GUNICORN (PRODUCTION)
# ========================================

# Number of worker processes (auto-calculated if not set)
# GUNICORN_WORKERS=4

# Number of threads per worker
# GUNICORN_THREADS=2

# Log level: debug, info, warning, error, critical
LOG_LEVEL=info

# ========================================
# SERVER (PRODUCTION)
# ========================================

# Port to run on (default: 5000)
# PORT=5000

# ========================================
# OPTIONAL FEATURES
# ========================================

# Email settings (for future features like password reset)
# MAIL_SERVER=smtp.gmail.com
# MAIL_PORT=587
# MAIL_USE_TLS=True
# MAIL_USERNAME=your-email@gmail.com
# MAIL_PASSWORD=your-app-password

# Sentry DSN (for error tracking)
# SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

